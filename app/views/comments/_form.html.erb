<br />
<strong>New comment</strong>
<%= form_for [@playlist, @comment] do |f| %>
 <p>
  <%= f.label :comment %>
  <%= f.text_area :comment, :size => "30x5" %>
</p><br>
<%= f.submit :class => 'btn' %>
<% end %>
